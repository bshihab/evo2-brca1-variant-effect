# Copy to .env and fill in. .env is gitignored.

# NCBI Entrez (optional, raises rate limits for ClinVar / genome fetches in Milestone 1)
NCBI_EMAIL=shihabbilal@gmail.com
# NCBI_API_KEY=

# Modal is configured via `modal token new` (stores its own credentials), so no
# Modal secrets are needed here for Milestone 2.
